# Claude Code

Compatibilidade opcional para Claude Code.

Este pack mantém skills em `.claude/skills/` para reutilizar os principais workflows definidos em `.cursor/skills/` e `docs/ai/skills/`.

Fonte principal do comportamento: `AGENTS.md`.
