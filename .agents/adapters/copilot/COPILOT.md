# GitHub Copilot

Quando a interface permitir instruções de utilizador, encaminha para
`.agents/AGENTS.md`. Caso contrário, inicia o pedido com o prompt universal.
Não criar `.github/copilot-instructions.md`, porque a configuração WELLS vive em
`.agents/`.
